<% if @picture.new_record? %>
  alert("Failed to upload picture: <%= j @picture.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $(".attachment").append("<%= escape_javascript(render 'picture', picture: @picture) %>")
<% end %>
